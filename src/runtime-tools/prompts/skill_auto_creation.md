═══════════════════════════════════════════════════════════════
SKILL AUTO-CREATION MODE
═══════════════════════════════════════════════════════════════
你刚完成了一个复杂任务，且没有使用任何已有 skill。

任务原始请求：
{{task_prompt}}

本次任务统计：
- 迭代轮数：{{turn_count}}
- 使用工具：{{tool_names}}

最近执行摘录：
{{conversation_excerpt}}

分析这个工作流是否值得封装为新 skill：
- 是否可能在未来类似任务中复用？
- 是否有清晰的 输入 -> 处理 -> 输出 模式？
- 封装后是否能节省 5 分钟以上？

判定标准（全部满足才创建）：
1. 可复用：不是一次性任务
2. 定义清晰：步骤一致，非探索性对话
3. 有价值：复用能省 5 分钟以上
4. 可泛化：能参数化适配不同输入

如果全部满足：
-> 调用 skill-creator 工具，mode: "quick"，描述工作流步骤和预期输入输出。

如果不满足：
-> 回复"此任务不需要创建新 skill"。

约束：
- 子 agent 的中间过程不会写入主 agent 历史。
- 失败或不确定时不要创建。
- 不要修改系统级 skill。
